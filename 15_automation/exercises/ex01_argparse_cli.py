"""
연습문제 01 — argparse CLI

TODO:
1. ArgumentParser 를 생성하고 --name (str) 과 --age (int) 옵션을 추가하라.
2. parser.parse_args(["--name", "Kee", "--age", "30"]) 를 호출하라.
3. args.name == "Kee" 이고 args.age == 30 임을 assert 로 검증하라.
4. args.age 의 타입이 int 임을 assert 로 검증하라.
"""

import argparse

# ── 여기에 코드를 작성하세요 ──


# 검증 (수정하지 마세요)
# assert args.name == "Kee"
# assert args.age == 30
# assert isinstance(args.age, int)
# print("OK")
