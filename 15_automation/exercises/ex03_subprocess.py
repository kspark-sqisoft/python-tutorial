"""
연습문제 03 — subprocess

TODO:
1. subprocess.run 으로 python -c "print('hi')" 를 실행하라.
   (capture_output=True, text=True 사용)
2. result.returncode == 0 임을 assert 로 검증하라.
3. result.stdout.strip() == "hi" 임을 assert 로 검증하라.
"""

import subprocess

# ── 여기에 코드를 작성하세요 ──


# 검증 (수정하지 마세요)
# assert result.returncode == 0
# assert result.stdout.strip() == "hi"
# print("OK")
