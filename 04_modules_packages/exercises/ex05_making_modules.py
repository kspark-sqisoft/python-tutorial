"""연습: 자체 모듈 import — _my_module 에서 double, triple 가져오기."""

# TODO: 같은 폴더의 _my_module 에서 double 과 triple 을 import 하라
# from _my_module import ...

assert double(5) == 10, "double(5) 는 10 이어야 합니다"
assert triple(4) == 12, "triple(4) 는 12 이어야 합니다"
print("OK")
